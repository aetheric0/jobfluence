services:
    -   type: web
        name: jobfluence-api
        env: python
        plan: free
        branch: master
        buildCommand: pip install -r requirements.txt
        startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
        routes:
            -   type: http
                path: /*
